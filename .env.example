# Copy to `.env` and adjust. Read by the serving layer (and docker-compose).
# Which damage model config to serve:
VI_DAMAGE_CONFIG=configs/models/yolov8_seg.yaml
# Path to your trained weights (after training). Leave unset to serve pretrained weights.
# VI_DAMAGE_WEIGHTS=runs/segment/yolov8s_cardd/weights/best.pt
