# Core runtime deps (mirror of pyproject [project].dependencies).
# For a full editable install with dev tools, prefer:  pip install -e ".[dev]"
ultralytics>=8.3.0
pydantic>=2.0,<3.0
pyyaml>=6.0
numpy>=1.24
pillow>=10.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
